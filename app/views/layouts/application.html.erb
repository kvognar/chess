<!DOCTYPE html>
<html>
<head>
  <title>RedditClone</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  
<h1><%= link_to "Reddit", subs_url %></h1>
  
<% if logged_in? %>
  <%= button_to "Log out",
    session_url,
    method: :delete,
    data: { confirm: "Are you sure you want to log out?" } %>
<% else %>
  <%= button_to "Log In",
    new_session_url,
    method: :get %>
  <%= button_to "Sign Up",
    new_user_url,
    method: :get %>
<% end %>



<%= yield %>

</body>
</html>
