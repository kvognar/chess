<h4><%= goal.title %></h4>
<h6><%= "Completed!" if goal.completed %></h6>
<p><%= goal.description %></p>
<% if current_user == goal.user %>
  <%= link_to "Update Goal", edit_goal_url(goal) %>
  <%= button_to "Delete Goal", goal_url(goal), method: :delete %>
<% end %>